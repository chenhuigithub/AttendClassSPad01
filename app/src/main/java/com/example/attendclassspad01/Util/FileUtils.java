package com.example.attendclassspad01.Util;

import android.os.Environment;

/**
 * 文件工具类
 */
public class FileUtils {
    //获得根目录/data (内部存储路径)
    public static String ROOT_DIRECTORY = Environment.getDataDirectory().getPath();
}
